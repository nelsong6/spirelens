# Issue 135 Red Mask Validation

Scenario: Ironclad with Red Mask loaded into `FUZZY_WURM_CRAWLER_WEAK`.

The captured `get_game_state` response showed `RED_MASK` on the player relic bar and one Fuzzy Wurm Crawler with `WEAK_POWER` amount 1.

![Red Mask tooltip](red-mask-tooltip.png)
