- name: Bootstrap Azure VMSS worker instance for CardUtilityStats
  hosts: vmss_workers
  gather_facts: false
  vars_files:
    - ../group_vars/windows_issue_agent_worker.yml
  roles:
    - role: windows_issue_agent_worker
      vars:
        windows_worker_expect_game_present: true
